# bird_species_prediction
Project demonstrates a bird species prediction model uses a pre-trained MobileNetV2 model, fine-tuned on a custom dataset of bird images. A web-based interface is built using Gradio to allow users to upload images and get real-time predictions. The interface includes a confidence threshold to indicate when the model is uncertain about a prediction.
